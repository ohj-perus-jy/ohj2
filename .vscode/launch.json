
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Zensical in Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:36742",
      "preLaunchTask": "zensical: serve",
      "webRoot": "${workspaceFolder}",
      "timeout": 20000
    }
  ]
}
